{
  "name": "tampermonkey-autofill",
  "version": "0.1.0",
  "private": true,
  "description": "Bibliotheque TypeScript pour preremplir des formulaires de candidature (SmartRecruiters, etc.) via Tampermonkey. Ne contient aucune donnee personnelle : le profil est fourni par le script Tampermonkey local de l'utilisateur.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  }
}
